<div align="center">
  <h1 align="center">PanelTone 安全策略</h1>
</div>

## 1. 支持范围

安全修复优先覆盖最新公开 alpha 与后续版本；尚未发布的本地开发分支不提供长期支持

## 2. 私密报告

请使用当前仓库 Security 页面的 GitHub 私密安全报告功能，并提供最小合成复现、受影响版本、预期与实际行为及建议缓解方式

报告中不要包含真实漫画、用户目录、访问令牌、模型服务密钥、完整日志或任务数据库；必须说明敏感值的位置时使用占位符

## 3. 默认安全边界

- Web 服务默认只监听 `127.0.0.1`
- 浏览器接口不返回服务器绝对路径
- ZIP 输入检查路径穿越、嵌套压缩包、成员数量与压缩比
- 永久删除只处理已经归档且位于任务根目录下的精确任务目录
- 模型下载需要用户发起，仓库不包含模型权重或缓存

## 4. 不属于安全保证的内容

生成结果的审美质量、人物一致性和模型输出偏差属于质量问题；如果偏差同时导致文件越界、隐私泄露或不安全执行，则按照安全问题报告
